# Data Generation Archive

The Harbor tasks in `samples/` use static, task-visible datasets. These scripts are retained only as reproducibility notes for how some synthetic inputs were originally produced; they are not used at task runtime.
